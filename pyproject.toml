[project]
name = "python-rest-flask"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dependency-injector>=4.48.1",
    "flask>=3.1.1",
    "python-dotenv>=1.1.1",
]
